name: Prepare Weekly

# Domingo: prepara o Recorte da Semana para disparar às 8h BRT.
# Fallbacks até 7:30 BRT (30 min antes do dispatch).

on:
  schedule:
    - cron: '0 8 * * 0'    # 05:00 BRT
    - cron: '30 8 * * 0'   # 05:30 BRT (fallback 1)
    - cron: '0 9 * * 0'    # 06:00 BRT (fallback 2)
    - cron: '30 9 * * 0'   # 06:30 BRT (fallback 3)
    - cron: '0 10 * * 0'   # 07:00 BRT (fallback 4)
    - cron: '30 10 * * 0'  # 07:30 BRT (fallback 5 — última chance antes do dispatch 8h)
  workflow_dispatch:

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: scripts/requirements.txt
      - name: Install deps
        working-directory: scripts
        run: pip install -r requirements.txt
      - name: Run prepare_weekly
        working-directory: scripts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          FEEDBACK_SECRET: ${{ secrets.FEEDBACK_SECRET }}
          FEEDBACK_BASE_URL: ${{ secrets.FEEDBACK_BASE_URL }}
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
          MANAGE_URL: ${{ secrets.MANAGE_URL }}
          PARALLEL_WORKERS: '5'
        run: python prepare_weekly.py
